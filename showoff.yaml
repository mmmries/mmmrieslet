apiVersion: v1
kind: ReplicationController
metadata:
  name: showoff
  labels:
    app: showoff
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: showoff
    spec:
      containers:
      - name: showoff
        image: hqmq/showoff:0.1.0
        imagePullPolicy: Always
        ports:
          - containerPort: 80
        resources:
          requests:
            cpu: 50m
            memory: 100M
---
apiVersion: v1
kind: Service
metadata:
  name: showoff
  labels:
    app: showoff
spec:
  type: NodePort
  ports:
  - port: 4000
  selector:
    app: showoff
